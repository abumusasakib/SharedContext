# Secrets — never commit these
.env
.env.*
mcp/shared-context-mcp/.env
mcp/shared-context-mcp/service-account.json
*.pem
*-key.json
*service-account*.json

# Dependencies
node_modules/

# Runtime cache / generated state
.cache/

# OS cruft
.DS_Store
._*
